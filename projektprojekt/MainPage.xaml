<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="projektprojekt.MainPage"
             BackgroundImageSource="start_bg.png">

    <ScrollView>
        <Grid
            WidthRequest="400"
            VerticalOptions="CenterAndExpand"
            HorizontalOptions="Center"
            RowSpacing="20"
            ColumnDefinitions="*,*,*"
            RowDefinitions="Auto,Auto,Auto">

            <ImageButton Source="startbtn_removebg_preview.png"
                   Grid.Row="0"
                   Grid.Column="1"
                   Aspect="AspectFit"
                   WidthRequest="180"
                   HeightRequest="72"
                   HorizontalOptions="Center"/>

            <ImageButton Source="options_removebg_preview.png"
                   Grid.Row="1"
                   Grid.Column="1"
                   Aspect="AspectFit"
                   WidthRequest="180"
                   HeightRequest="72"
                   HorizontalOptions="Center"
                   Clicked="ImageButton_Clicked_1"/>

            <ImageButton Source="exitbtn_removebg_preview.png"
                   Grid.Row="2"
                   Grid.Column="1"
                   Aspect="AspectFit"
                   WidthRequest="180"
                   HeightRequest="72"
                   HorizontalOptions="Center" Clicked="ImageButton_Clicked"/>

        </Grid>
    </ScrollView>

</ContentPage>
